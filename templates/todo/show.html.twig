{% extends "base.html.twig" %}

{% block body %}
	<main>
		<section class="container bg-light">
			<p class="btn btn-danger my-5">To Do list</a>
			<div class="row">
				<p>{{ todo.title }}</p>
				<p>{{ todo.category.name}}</p>
				<p>{{ todo.createdAt|date('d/m/Y') }}</p>
				<p>{{ todo.content }}</p>

			</div>
		</section>
	</main>
{% endblock %}
